task 0 (preprocessor) - runs a c file through the preprocessor and saves the result into another file.

Task 1 (compiler) - compiles a c file but doesn't link and the output should be named the same as the C file but replaces the .c with .o

Task 2 (assembler) - generates the assemble code of a c file and saves  it in a file named as the original file but replaces the .c with .s


